{
  "name": "edlink-js",
  "version": "2.0.0",
  "license": "MIT",
  "description": "NodeJS wrapper for the Edlink API.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "test": "yarn build && yarn jest",
    "format": "prettier --write \"lib/**/*.ts\"",
    "circular-deps": "node_modules/madge/bin/cli.js --warning --circular --extensions ts ./"
  },
  "contributors": [
    "Dakota Gordon <dakota@ed.link>",
    "Chris McLain <chris@ed.link>"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/url-join": "^4.0.1",
    "jest": "^27.1.0",
    "madge": "^5.0.1",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "url-join": "^4.0.1"
  }
}
